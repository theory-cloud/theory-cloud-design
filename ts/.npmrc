# npm 12 defaults allow-remote=none. This package intentionally consumes the
# pinned FaceTheory release tarball. Keep the narrow root policy: npm may
# fetch only URL dependencies declared by ts/package.json.
allow-remote=root
