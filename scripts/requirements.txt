# Python toolchain for `scripts/build-assets.py`.
#
# Pinned so CI and release workflows regenerate byte-identical raster
# outputs and the "committed rasters match SVG masters" parity check
# stays stable across runs. Bump both packages intentionally in a
# scoped commit; drift here can produce spurious parity failures.
cairosvg==2.9.0
Pillow==12.2.0
